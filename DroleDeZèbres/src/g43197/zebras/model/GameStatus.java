package g43197.zebras.model;

/**
 * Class for representation of game status, so actions are always done in a good
 * order.
 *
 * @author Philippe
 */
public enum GameStatus {
    INIT, ANIMAL, IMPALA;
}
